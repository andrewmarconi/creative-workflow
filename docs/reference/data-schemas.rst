Data Schemas (JSON)
===================

.. admonition:: Stub
   :class: warning

   This page is a placeholder. Content to be written.

Field-by-field specification for all JSON data files.

Planned content:

- ``data/presets.json`` — Model and LoRA configuration
- ``data/llm_models.json`` — LLM model definitions
- ``data/regions.json`` — Geographic regions
- ``data/countries.json`` — Countries with default language references
- ``data/languages.json`` — Languages with LLM model references
- ``data/country_regions.json`` — Country-to-Region M2M mappings
- ``data/country_languages.json`` — Country-to-Language M2M mappings
- ``data/prompt_templates.json`` — LLM prompt templates
- ``data/segments.json`` — Audience segments
- ``data/personas.json`` — Audience personas
- ``data/persona_segments.json`` — Persona-to-Segment M2M mappings
- ``data/brands.json`` — Brand reference data
- ``data/example_tvspot.json`` — TV spot import format

Each schema: field names, types, required/optional, example values.
