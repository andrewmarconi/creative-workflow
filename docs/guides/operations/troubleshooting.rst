Troubleshooting
===============

.. admonition:: Stub
   :class: warning

   This page is a placeholder. Content to be written.

Common issues and their solutions.

Planned content:

- **Model loading issues**: OOM errors, wrong dtype, missing model files
- **LoRA issues**: incompatible architecture, missing trigger words, download failures
- **CivitAI download problems**: API key, rate limits, invalid AIR URN
- **Celery task errors**: tasks stuck in pending, worker crashes, timeout issues
- **Pipeline failures**: evaluation gate loops, model loading errors, schema validation
- **Django admin issues**: missing static files, migration errors, permission problems
- **Docker/infrastructure**: port conflicts, container startup order, volume permissions
- **Common Python errors**: ``ModuleNotFoundError: 'cw'`` (run ``uv pip install -e .``)
