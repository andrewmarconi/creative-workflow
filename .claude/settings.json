{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'INPUT=$(cat); if echo \"$INPUT\" | grep -q \"git commit\"; then python3 \"$CLAUDE_PROJECT_DIR/.claude/hooks/consistency_checker.py\" --repo \"$CLAUDE_PROJECT_DIR\" >&2 || true; fi'"
          }
        ]
      }
    ]
  }
}
